#include <stdio.h>

int main()
{
	//Declare the variable num here
    int num;
    num = 5;
	//Assign value of 5 to the variable num here


	printf("%d", num);//Note that %d is used print an integer and after a
	return 0;		//comma, the integer to be printed.
}
