/*
    if - else statement:
        -> Syntax:
            if(expression / constant[Zero/Non-Zero] / variable)
            {
                -statements1
            }
            else
            {
                -statements2
            }
        -> Execution:
            -> if expression value is true/non-zero statements1 will be executed
            -> if expression value is false/zero statements2 will be executed
            -> Note: Only one statement will be executed [Either stmt1 or stmt2]
        -> if - else can be called as two-way branching statement
*/
